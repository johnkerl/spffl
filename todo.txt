================================================================
k cmake (non-install) structure
  k initial lib skeleton
  k initial test skeleton
  k initial exe skeleton
k formatting pass
  clang-format -i $(find src  -name '*.h' -o -name '*.cpp')
  clang-format -i $(find cli  -name '*.h' -o -name '*.cpp')
  clang-format -i $(find test -name '*.h' -o -name '*.cpp')
k pointer -> reference pass
k namespacing pass: int_gcd -> spffl::intmath::int_gcd etc
k int -> bool pass -- done??
k manual clang-format
  - needs automated/CI clang-format

* renaming pass
  - needs more
  - other unabbrevs

* no-malloc case for f2poly_t w/ <= 64 bits, and expand-to-malloc on overflow

* char* vs string literals
* int -> bool pass
  - needs more

* unsigned -> uint32_t etc pass
  o intmath:
    - UTs for numbers over 2^32
    - is_prime/nth_prime abend beyond 2^31-1
  o bits
    - bit_vector_t
  o polynomials
    - f2_poly_t 64-bit parts

* renaming pass
  o needs more
  o get_char -> get_characteristic
  o other unabbrevs

* ctest pass(es)
* commenting pass
* rid of _1 _2 etc in test names
* more constness
* STL replacements
  o get rid of rsstring_t entirely
  o pointer+length to std::vec/std::span
  o bitvec to STL -- ?
  o more std::vec
  o ...
* exit(1) -> exceptions
* core-guidelines pass
* install target
* internal/external installs (if any internal api at all ...)
* CI setups

----------------------------------------------------------------
./build/test/bits/test_bits --list-tests
./build/test/bits/test_bits

----------------------------------------------------------------
TBF

  ./classes/tmvpoly.h
  ./classes/tmvrat.h
  ./classes/teiginfo.h

  ./utils/moebius.h

  ./utils/fp_scan_tuples.h
  ./utils/ft_scan_tuples.h
  ./utils/tmvpolyscan.h
  ./utils/tmvratscan.h
